let cors,express,readdirSync,mongoose;_2be‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_2be‍.w("express",[["default",["express"],function(v){express=v}]]);_2be‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_2be‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



const morgan = require("morgan");
require("dotenv").config();

// create express app
const app = express();

// db
mongoose
  .connect(process.env.DATABASE
  )
  .then(() => console.log("**DB CONNECTED**"))
  .catch((err) => _2be‍.g.console.log("DB CONNECTION ERR => ", err));

// apply middlewares
app.use(cors());
app.use(express.json());
app.use(morgan("dev"));


//connection routes dynamicllay
readdirSync("./routes").map((r)=>app.use("/api", require(`./routes/${r}`)));

// port
const port = process.env.PORT || 5000;

app.listen(port, () => console.log(`Server is running on port ${port}`));
